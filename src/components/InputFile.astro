---

---
<input type="file" name="multimedia" id="multimedia"
  class="filepond hidden"
  disabled 
  multiple 
  accept="image/webp, image/jpeg, image/png, image/gif" 
  data-max-file-size="3MB" 
  data-max-files="3"/>

<!-- Estilos y Scrip -->
<style is:global>
  .filepond--drop-label {
    background-color: transparent !important;
  }

  .filepond--panel-root {
    background-color: transparent !important;
    border: 1px dashed rgba(128, 128, 128, 0.688);
  }

  .filepond--label-action {
    color: #38bff8ab !important; /* sky-400 */
    text-decoration: none;
  }
</style>

<script type="module">
  // Registrar los plugins que necesitas
  FilePond.registerPlugin(
    FilePondPluginImagePreview,
    FilePondPluginFileValidateSize,
    FilePondPluginFileValidateType
  );

  // Inicializar FilePond en el input con ID "multimedia"
  FilePond.create(document.getElementById('multimedia'), {
    credits: false,
    acceptedFileTypes: ['image/webp', 'image/jpeg','image/png','image/gif'],
    allowMultiple: true,
    labelFileTypeNotAllowed: 'Solo se permiten imágenes png, jpeg o gif',
    labelIdle: '🌄​ Arrastra tus imágenes aquí o haz click para añadir',
    fileValidateTypeLabelExpectedTypes: 'Debe ser una imagen png, gif o jpeg',
    instantUpload: false //espera al submit
  });

</script>