---
import { use } from "react";
import Layout from "../../layouts/Layout.astro";
import LayoutGrid from "../../layouts/LayoutGrid.astro";

const user = await Astro.locals.currentUser()
console.log(user?.hasImage);

---
<Layout>
  <LayoutGrid>
    <div class="flex mt-5 justify-around p-2">
      <div>
        <h2 class="text-2xl font-bold text-center text-pretty">{user?.fullName}</h2>
        <p>{/* JSON.stringify(user?.createdAt) */}</p>
      </div>
      <img src={user?.imageUrl} class=" size-52 md:size-60 object-cover transition-all" alt="">
    </div>
  </LayoutGrid>
</Layout>